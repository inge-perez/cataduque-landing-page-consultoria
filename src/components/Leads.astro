---
import { BRAND, WHATSAPP_MSG } from '../layouts/layout.astro';
---

<section id="form" class="py-20 bg-gray-50">
    <div
        class="container mx-auto px-6 max-w-5xl grid md:grid-cols-2 gap-16 items-center"
    >
        <!-- Lado izquierdo -->
        <div>
            <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900">
                Agenda tu asesoría gratuita
            </h2>
            <p class="text-gray-600 mb-6">
                Completa el formulario y te contactaremos en menos de
                <strong>24 horas hábiles</strong>. También puedes escribirnos
                directamente por WhatsApp.
            </p>

            <div class="flex gap-4 flex-wrap">
                <a
                    class="bg-violet-600 text-white px-6 py-3 rounded-xl shadow-md hover:bg-violet-700 transition animate-bounce-slow"
                    href={`https://wa.me/${BRAND.whatsapp}?text=${WHATSAPP_MSG}`}
                    target="_blank"
                    rel="noopener"
                >
                    📲 Escribir por WhatsApp
                </a>
                <a
                    target="_blank"
                    rel="noopener"
                    href="https://nas.io/hrboost.academy"
                    class="px-6 py-3 rounded-xl border border-tertiary text-tertiary font-semibold shadow-md hover:bg-gray-100 transition"
                >
                    🎁 Únete gratis a la Comunidad HR Boost
                </a>
            </div>
        </div>

        <!-- Formulario -->
        <form
            id="leadForm"
            class="bg-white shadow-lg rounded-2xl p-8 space-y-6 border border-gray-100"
        >
            <div class="grid md:grid-cols-2 gap-6">
                <div>
                    <label
                        for="name"
                        class="block text-sm font-medium text-gray-700"
                    >
                        Nombre
                    </label>
                    <input
                        id="name"
                        name="name"
                        type="text"
                        placeholder="Tu nombre"
                        required
                        class="mt-1 w-full border rounded-lg p-3 focus:ring-violet-500 focus:border-violet-500"
                    />
                </div>
                <div>
                    <label
                        for="email"
                        class="block text-sm font-medium text-gray-700"
                    >
                        Email
                    </label>
                    <input
                        id="email"
                        name="email"
                        type="email"
                        placeholder="tu@email.com"
                        required
                        class="mt-1 w-full border rounded-lg p-3 focus:ring-violet-500 focus:border-violet-500"
                    />
                </div>
            </div>

            <div>
                <label
                    for="phone"
                    class="block text-sm font-medium text-gray-700"
                >
                    Teléfono / WhatsApp
                </label>
                <input
                    id="phone"
                    name="phone"
                    type="tel"
                    placeholder="57 3xx xxx xxxx"
                    required
                    class="mt-1 w-full border rounded-lg p-3 focus:ring-violet-500 focus:border-violet-500"
                />
            </div>

            <!-- Nuevo campo para segmentar -->
            <div>
                <label
                    for="challenge"
                    class="block text-sm font-medium text-gray-700"
                >
                    Tu principal reto en RRHH (opcional)
                </label>
                <select
                    id="challenge"
                    name="challenge"
                    class="mt-1 w-full border rounded-lg p-3 focus:ring-violet-500 focus:border-violet-500"
                >
                    <option value="">Selecciona una opción</option>
                    <option value="retencion">Retención de talento</option>
                    <option value="clima">Mejora del clima laboral</option>
                    <option value="contratacion"
                        >Optimizar procesos de contratación</option
                    >
                    <option value="otro">Otro</option>
                </select>
            </div>

            <div>
                <label
                    for="message"
                    class="block text-sm font-medium text-gray-700"
                >
                    Mensaje (opcional)
                </label>
                <textarea
                    id="message"
                    name="message"
                    rows="3"
                    placeholder="Cuéntanos brevemente tu necesidad"
                    class="mt-1 w-full border rounded-lg p-3 focus:ring-violet-500 focus:border-violet-500"
                ></textarea>
            </div>

            <!-- Feedback dinámico -->
            <div
                id="formMessage"
                role="status"
                aria-live="polite"
                class="hidden text-sm mt-2"
            >
            </div>

            <div class="flex flex-col items-start gap-3">
                <button
                    type="submit"
                    class="bg-teal-600 text-white px-6 py-3 rounded-xl shadow-md hover:bg-teal-700 transition w-full md:w-auto"
                >
                    🚀 Quiero mi diagnóstico
                </button>
                <small class="text-gray-500 text-sm">
                    ✅ Respuesta garantizada en menos de 24h hábiles. Al enviar
                    aceptas nuestra política de privacidad.
                </small>
            </div>
        </form>
    </div>
</section>

<script>
    import '../utils/sendNotification';
</script>

<style>
    /* Animación más sutil que bounce normal */
    .animate-bounce-slow {
        animation: bounce 3s infinite;
    }
</style>
